{
  "name": "ryvoki-mail-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "schema": "wrangler d1 execute ryvoki-db --remote --yes --file=./schema.sql",
    "schema:local": "wrangler d1 execute ryvoki-db --local --file=./schema.sql"
  },
  "dependencies": {
    "postal-mime": "^3.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260914.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.131.2"
  }
}
